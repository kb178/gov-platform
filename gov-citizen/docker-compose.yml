version: '3.8'

services:
  gov-citizen:
    build: .
    container_name: gov-citizen
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - gov-network

networks:
  gov-network:
    driver: bridge
